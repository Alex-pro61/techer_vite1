
import { Container, Typography } from '@mui/material';

const TeacherDashboard = () => {
  return (
    <Container>
      <Typography variant="h4">Teacher Dashboard</Typography>
      {/* Додайте інші елементи інтерфейсу тут */}
    </Container>
  );
};

export default TeacherDashboard;
